<h2>Edit brewery</h2>

<%= render "form.html", changeset: @changeset,
                        action: brewery_path(@conn, :update, @brewery) %>

<span><%= link "Back", to: brewery_path(@conn, :index) %></span>
