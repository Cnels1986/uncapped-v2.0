<h1>Registered Beers</h1>

<table class="table">
  <%= for beer <- @beers do %>
    <tr>
      <td><%= beer.name %></td>
      <td><%= beer.type %></td>
      <td><%= link "Rate this beer", to: beer_path(@conn, :show, beer.id) %></td>
    </tr>
  <% end %>
</table>
